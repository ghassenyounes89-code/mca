{
  "name": "mca-root",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "cd mca && npm run dev",
    "build": "cd mca && npm ci && npm run build && rm -rf ../dist || true && mkdir -p ../dist && cp -r dist/* ../dist/",
    "preview": "cd mca && npm run preview"
  },
  "dependencies": {
    "axios": "^1.13.2"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.22",
    "postcss": "^8.5.6",
    "tailwindcss": "^4.1.17"
  }
}
